#include "Wall.h"
#include <stdlib.h>
#include <time.h>
#include "GlobalVariable.h"

Wall::Wall()
{
	/*srand(time(NULL));
	int posX = rand() % XLine;
	int posY = rand() % YLine;

	this->SetPos({posX, posY});*/
}